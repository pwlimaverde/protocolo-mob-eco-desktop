name: login_module
description: Modulo responsável pelo gerenciamento do login da aplicação.
publish_to: 'none'
version: 0.0.1
homepage:

environment:
  sdk: '>=2.18.4 <3.0.0'
  flutter: ">=3.3.8"

dependencies:
  flutter:
    sdk: flutter
  dependencies_module:
    path: ../dependencies_module

dev_dependencies:

  flutter_lints: ^2.0.0

flutter:
